export enum routes {
  interviews = "/interviews",
  interview = "/interviews/:id",
  edit = "/interviews/:id/edit",
  reset = "/interviews/:id/reset",
  projects = "/projects",
  project = "/projects/:project",
  atoms = "/atoms",
  nodes = "/atomnodes",
  textblock = "/textblocks/:id",
  textblocks = "/textblocks",
}
